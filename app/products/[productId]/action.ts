// import axios from "axios";

// export async function getProduct(productId: string) {
//   const baseUrl =
//     process.env.NEXT_PUBLIC_API_BASE_URL || "http://localhost:3000";
//   const product = await axios.get(`${baseUrl}/api/products/${productId}/get`);
//   return product; // Return just the data, not the entire Axios response
// }
